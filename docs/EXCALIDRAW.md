# Open the architecture in Excalidraw

Excalidraw.com does not give a public create-share API, so there is no live `excalidraw.com/#json=...` link from this repo.

## File
`docs/privis-architecture-user-return.excalidraw`

If that file still says PLACEHOLDER, use the copy from the teammate chat download.

## Open
1. Go to https://excalidraw.com
2. Menu (hamburger) → **Open** → pick the `.excalidraw` file
3. Or drag the file onto the canvas

## What was added vs the notebook drawing
- Green **USER GETS** box: live page updates + HUD
- Green arrow: Local Executor → user tab (`page updates on the REAL tab`)
- Dashed blue arrow: Human Approval → user (`ask user yes/no`)
- No new architecture box name
