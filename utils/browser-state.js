// utils/browser-state.js — builds the BrowserState part of the capture package.

// Shape: { url, title, viewport: {w,h}, forms: [{ fields: [{ id, type, label }] }] }
// Field VALUES are not included — labels and types only.
function buildBrowserState(pageState) {
  // TODO
  return null;
}

export { buildBrowserState };
